stages:
  - code-check

flake8_code:
  stage: code-check
  image: python:3
  before_script:
    - python3 -m venv venv
    - source venv/bin/activate
    - pip install -r requirements.txt
  script:
    - flake8 --max-line-length=150 --extend-exclude=*.pyc,*__init__.py,./venv/*, ./